# CATL(3750.HK) 차트 검증 메모

이미지에 표시된 숫자를 기준으로 산술 일관성을 검증했다.

## 핵심 결론

- **Heatmap/막대 값은 대부분 `주가 = PER × EPS` 산식과 부합**한다.
- 다만 일부 셀은 **반올림/표기 규칙 차이(±1 HKD)**가 보인다.
- 가장 큰 불일치는 다음이다.
  - `PE 27 × EPS 18.26`의 기계 계산은 **493 HKD**인데,
  - 차트의 `Final Reported (PE 27)` 막대는 **511 HKD**로 표시됨.
  - 즉, 해당 511은 `EPS 18.26`이 아니라 대략 `EPS 18.93` 수준에 대응한다.

## 실행 방법

```bash
python3 analysis_validate.py
```

## 검증 포인트

1. EPS 평균/표준편차 범위 계산(2026E~2028E)
2. Heatmap의 `PE 24x` 행 재계산
3. 주요 타깃(387/511/591/493) 역산
4. 500 HKD 기준 수익률 라벨(+/-%) 재확인

